import { MarketsHubPage } from "@/components/search/MarketsHubPage";

export const metadata = {
  title: "Marchés · Change & carburants · AYEBA",
  description: "Taux de change, essence, gasoil, gaz et crypto — convertisseur RDC.",
};

export default function MarchesPage() {
  return <MarketsHubPage />;
}
